#include <iostream> // Bibliotecas/llibrerias
#include <string>

using namespace std;    //std per no tenir que posar std:cout 
   //COMENTARIS es pot fer aixi // o aixi /* */
int main()
{
    //assignament de variables
        //Int numeros
        //Strings Frases/lletra
        //char caracters [numero de caracters]                  //ESTRUCTURA
    int variableNum1 = 0; // Declarada i assignada
    int variableNum2;     // Declarada
    string variableFrase1;
    if (/*condicio*/) {
        //lo que vulguis que fagi el if
    }

    return 0;
}
