// ProjecteVar.cpp : Este archivo contiene la función "main". La ejecución del programa comienza y termina ahí.
//double numeros amb comas

#include <iostream>
#include <string>
//<SFML>



using namespace std;


int main()
{


    return 0;
    /*
    string nom;
    int b, a, c, t;

    t = b * b - 4 * a * c;
    */










    

    /*
    int caramels;
    double onzas;

        cout << "Entra el numero de barras de caramel en cada paquet i la mida en onzas de cada barra de caramelo. \n";
        cout << "press return ";
        cout << "Entra el numero de caramels: \n";
        cin >> caramels;
        cout << "Entra el numero de onzas: \n";
        cin >> onzas;
        cout << "caramels = " << caramels << "\n";
        cout << "onzas = " << onzas << "\n";
        double pesoTotal = caramels * onzas;
        cout << "peso total = " << caramels << " * " << onzas << " = " << pesoTotal << "\n";

        */










    
    /*
    int number_of_pods, peas_per_pod, total_peas;

    cout << "Press return after entering a number.\n"; 
    cout << "Enter the number of pods: \n";
    cin >> number_of_pods;
    cout << "Enter the number of peas in a pod:\n";
    cin >> peas_per_pod;
    total_peas = number_of_pods * peas_per_pod;
    cout << "If you have ";
    cout << number_of_pods; cout << " pea pods \n"; cout << "and ";
    cout << peas_per_pod;
    cout << " peas in each pod, then\n"; cout << "you have ";
    cout << total_peas;
    cout << " peas in all the pods.\n";  
    */
    
    /*
    int Valor1;
    int Valor2;
    cout << "Di un valor per sumar" << endl;
    cin >> Valor1;
    cout << "Digues un altre valor" << endl;
    cin >> Valor2;
    cout << "El Valor de " << Valor1 << " + " << Valor2 << " Es " << Valor1 + Valor2;
    */

    /*
    int N1;
    int N2;
    cout << "Digues un numero" << endl;
    cin >> N1;
    cout << "Digues un altre numero" << endl;
    cin >> N2;

    cout << N1 << " + " << N2 << " = " << N1 + N2 << endl;
    cout << N1 << " - " << N2 << " = " << N1 - N2 << endl;
    cout << N1 << " * " << N2 << " = " << N1 * N2 << endl;
    cout << N1 << " / " << N2 << " = " << N1 / N2 << endl;

    
    cout << "Digues un numero amb decimal" << endl;
    cin >> N1;
    cout << "Digues un altre numero amb decimal" << endl;
    cin >> N2;
    cout << N1 << " / " << N2 << " = " << N1 / N2 << endl;
    cfml
     */
   
   
}

// Ejecutar programa: Ctrl + F5 o menú Depurar > Iniciar sin depurar
// Depurar programa: F5 o menú Depurar > Iniciar depuración

// Sugerencias para primeros pasos: 1. Use la ventana del Explorador de soluciones para agregar y administrar archivos
//   2. Use la ventana de Team Explorer para conectar con el control de código fuente
//   3. Use la ventana de salida para ver la salida de compilación y otros mensajes
//   4. Use la ventana Lista de errores para ver los errores
//   5. Vaya a Proyecto > Agregar nuevo elemento para crear nuevos archivos de código, o a Proyecto > Agregar elemento existente para agregar archivos de código existentes al proyecto
//   6. En el futuro, para volver a abrir este proyecto, vaya a Archivo > Abrir > Proyecto y seleccione el archivo .sln
